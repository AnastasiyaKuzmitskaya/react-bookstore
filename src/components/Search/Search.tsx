import React from "react";

export const Search = () => {
  return {
    /* <StyledSearch onSubmit={handleSearch}>
      <Input
        type="text"
        placeholder="Search..."
        name="search"
        {...searchValue}
      />
      <Button type="submit">
        <SearchLogoIcon />
      </Button>
    </StyledSearch> */
  };
};
